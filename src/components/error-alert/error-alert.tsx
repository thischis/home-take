import React from 'react';
import { Alert } from 'reactstrap';

const ErrorAlert: React.FC = () => {
  return <Alert color="danger">Ooops! It`s seems like some error occurs</Alert>;
};

export default ErrorAlert;
